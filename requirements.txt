# Requirements for Heterodyne Detector
# Install with: pip install -r requirements.txt

# CRITICAL: SDR Communication Library
# Use pyadi-iio (more stable and recommended)
pyadi-iio>=0.0.17

# Alternative (if you must use libiio directly):
# Build libiio from source: https://github.com/analogdevicesinc/libiio
# Then: pip install pylibiio

# Signal processing
numpy>=1.21.0
scipy>=1.7.0

# GPU acceleration (optional but recommended for RX 6700 XT)
# For AMD GPU: Install ROCm first, then CuPy
# Check ROCm version with: rocm-smi
# cupy-rocm-5-0>=11.0.0  # Uncomment and adjust version for your ROCm

# Pattern matching
dtaidistance>=2.3.10  # DTW implementation with C speedups
python-Levenshtein>=0.20.0  # Fast Levenshtein distance

# Visualization (optional)
matplotlib>=3.5.0
# plotly>=5.0.0  # Uncomment for interactive plots

# Audio analysis (for phantom voice detection)
# librosa>=0.9.0  # Uncomment if needed
# soundfile>=0.11.0

# Parallel processing
joblib>=1.1.0

# Data handling
pandas>=1.3.0
# h5py>=3.6.0  # Uncomment for saving large datasets
